name: Train Model

on:
  push:
    branches:
      - main
  workflow_dispatch:  # Ermöglicht manuelles Triggern des Workflows

jobs:
  train:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository content
        uses: actions/checkout@v2
        with:
          lfs: true  # Git LFS aktivieren
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install accelerate -U  # Beschleuniger aktualisieren, wenn erforderlich

      - name: Run training script
        run: |
          python train_model.py

      - name: Commit and push trained model
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add trained_model/*
          git commit -m 'Add trained model'
          git pull --rebase origin main  # Änderungen vom Remote-Repository ziehen und integrieren
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.LMM_Token }}
